import React, { useState, useEffect } from 'react'
import { useStore } from '../../store'

const Live = ({classes, history}) => {
  //TODO: socket logic
  return(
    <div>
      LIVE SCREENS
    </div>
  )
}

export default Live
