const styles = (theme) => ({
  container: {
    display: 'flex',
    flexDirection: 'column',
    minHeight: '100%'
  },
  btnWrapper: {
    width: '90%'
  },
  btn: {
    width: '100%'
  }
});

export default styles;
